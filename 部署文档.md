好，那我按“**Docker + 云服务器完整上线**”给你一套**可以照着一步一步做的落地流程**（从0到可访问）。

我会尽量写成“执行手册”，你照做就能跑起来。

---

# 一、准备阶段（本地）

## 1️⃣ 确认项目结构

建议整理成：

```
project/
├── frontend/        # 项目
├── backend/         # Python API
├── nginx/
│   └── nginx.conf
└── docker-compose.yml
```

---

## 2️⃣ 前端打包测试

进入 frontend：

```bash
npm install
npm run build
```

生成：

```
frontend/dist/
```

👉 用浏览器打开 `index.html`，确认页面正常

---

## 3️⃣ 后端本地跑通

进入 backend：

```bash
pip install -r requirements.txt
```

运行：

```bash
uvicorn main:app --reload
```

测试：

```
http://localhost:8000/docs
```

👉 必须确保 API 正常

---

# 二、写 Docker（核心）

---

## 4️⃣ 后端 Dockerfile（backend/）

```dockerfile
FROM python:3.10

WORKDIR /app

COPY . .

RUN pip install --no-cache-dir -r requirements.txt

CMD ["gunicorn", "-k", "uvicorn.workers.UvicornWorker", "main:app", "-b", "0.0.0.0:8000"]
```

---

## 5️⃣ Nginx 配置（nginx/nginx.conf）

```nginx
events {}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }

        location /api/ {
            proxy_pass http://backend:8000/;
        }
    }
}
```

---

## 6️⃣ docker-compose.yml（项目根目录）

```yaml
version: '3.9'

services:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./frontend/dist:/usr/share/nginx/html
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: backend
    restart: always
    depends_on:
      - db

  db:
    image: mysql:8
    container_name: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: schedule
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
```

---

# 三、购买服务器

选择：

* Alibaba Cloud
* 或 Tencent Cloud

---

## 7️⃣ 服务器配置

推荐：

* 系统：Ubuntu 22.04
* 配置：2核4G

---

## 8️⃣ 开放端口（非常重要）

在云控制台放行：

```
80   (HTTP)
443  (HTTPS，后面用)
22   (SSH)
```

---

# 四、服务器初始化

## 9️⃣ 登录服务器

```bash
ssh root@你的服务器IP
```

---

## 🔟 安装 Docker

```bash
apt update
apt install docker.io -y
```

启动：

```bash
systemctl start docker
systemctl enable docker
```

---

## 11️⃣ 安装 docker-compose

```bash
apt install docker-compose -y
```

---

# 五、部署项目

---

## 12️⃣ 上传代码

方式1（推荐）：

```bash
git clone 你的仓库
cd project
```

方式2：scp上传

---

## 13️⃣ 启动项目

```bash
docker-compose up -d --build
```

---

## 14️⃣ 查看运行状态

```bash
docker ps
```

---

## 15️⃣ 测试访问

浏览器打开：

```
http://你的服务器IP
```

👉 如果成功，你的网站已经上线

---

# 六、绑定域名（可选但建议）

## 16️⃣ 买域名

任意平台购买

---

## 17️⃣ 域名解析

把域名指向服务器IP

---

# 七、HTTPS（必须做）

安装证书工具：

```bash
apt install certbot python3-certbot-nginx -y
```

执行：

```bash
certbot --nginx
```

👉 自动配置 HTTPS

---

# 八、数据库连接（关键点）

你的后端连接 MySQL：

```python
host = "db"
user = "root"
password = "123456"
database = "schedule"
```

⚠️ 注意：

* 不能写 localhost
* Docker里服务名就是主机名

---

# 九、常见问题（你一定会遇到）

## ❗ 1. 前端请求失败

👉 原因：API地址写错

正确写法：

```js
/api/xxx
```

---

## ❗ 2. 跨域问题

👉 Nginx代理已经解决（不要直接访问8000端口）

---

## ❗ 3. 数据库连不上

👉 检查：

* 用户名密码
* host 是否是 `db`

---

## ❗ 4. 容器挂了

```bash
docker logs backend
```

---

# 十、后期维护

## 更新代码

```bash
git pull
docker-compose up -d --build
```

---

## 重启

```bash
docker-compose restart
```

---

## 停止

```bash
docker-compose down
```

---

# 十一、你现在下一步该做什么

按顺序：

1. ✔ 本地跑通前后端
2. ✔ 写好 Dockerfile + docker-compose
3. ✔ 买服务器
4. ✔ 部署运行

---

# 如果你想再往下一步（我可以帮你）

我可以直接帮你做这些“进阶优化”：

* ✅ 文件上传（PDF存储方案）
* ✅ OCR服务拆分
* ✅ Redis缓存
* ✅ 用户系统（登录注册）
* ✅ 日志 + 监控

---

如果你愿意，可以把你的：
👉 backend 目录结构
👉 main.py

发我，我可以帮你**把 docker 配置直接改成可运行版本**，避免你踩坑。
