# 环境配置说明

## 端口分配

| 服务 | Docker环境 | 本地开发环境 |
|------|------------|--------------|
| Web应用 | 3000 | 3001 |
| AI服务 | 5002 | 5003 |
| MySQL | 3307 | 3306 |

## 使用方式

### 方式1：使用Docker完整环境（推荐）

```bash
# 启动所有Docker服务
docker-compose up -d

# 访问 http://localhost:3000
```

### 方式2：本地开发 + Docker数据库和AI

```bash
# 启动Docker的数据库和AI服务
docker-compose up db ai-service -d

# 复制Docker环境变量
copy .env.docker .env

# 启动本地Web应用
npm run dev

# 访问 http://localhost:3001
```

### 方式3：完全本地开发

```bash
# 1. 停止Docker服务
docker-compose stop

# 2. 复制本地环境变量
copy .env.local .env

# 3. 修改 service.py 端口为 5003
# 编辑 service.py 第300行：app.run(host='0.0.0.0', port=5003, debug=True)

# 4. 启动本地AI服务（需要Python环境）
python service.py

# 5. 启动本地Web应用
npm run dev

# 访问 http://localhost:3001
```

## 环境变量文件

- `.env` - 当前使用的环境变量
- `.env.docker` - Docker环境的配置
- `.env.local` - 本地开发环境的配置

## 切换环境

### 从本地切换到Docker：
```bash
copy .env.docker .env
docker-compose up -d
```

### 从Docker切换到本地：
```bash
copy .env.local .env
npm run dev
```
